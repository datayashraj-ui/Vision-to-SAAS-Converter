# Components

Reusable UI components used throughout the application.

Examples:
- Layout components (Header, Footer, Sidebar)
- Form components (Input, Button, Select)
- Chat components (Message, MessageList, ChatInput)
- Common UI elements
